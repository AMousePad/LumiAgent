List the user's configured LLM connection profiles. Returns id, name, provider, api_url, model, is_default, has_api_key (boolean — never the actual key). Use to see what providers/models are available, or to figure out which connection a chat is using.